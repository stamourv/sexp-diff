#lang setup/infotab

(define name "sexp-diff")
(define blurb
  (list "S-expression-aware diffing tool based on Levenshtein-like tree edit distance."))
(define scribblings '(["sexp-diff.scrbl" ()]))
(define categories '(devtools datastructures))
(define primary-file "main.rkt")
(define release-notes (list))
(define repositories '("4.x"))
